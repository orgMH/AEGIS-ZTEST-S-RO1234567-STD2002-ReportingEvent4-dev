############################################################
# タイトル: 基礎的なRによる簡単なデータ解析スクリプト
# 作成日: 2026-02-22
# 作成者: ruser3
#
# 更新履歴:
# 2026-02-22: 初版作成
# 2026-02-25: プロットを使用しないように変更
############################################################

# ---- 1. データ作成 ----
x <- 1:5
df <- data.frame(
  value   = x,
  squared = x^2
)

# ---- 2. 集計 ----
mean_value <- mean(df$value)
sd_value   <- sd(df$value)

# ---- 3. 結果表示（標準出力に出す） ----
cat("mean:", mean_value, "\n")
cat("sd  :", sd_value, "\n")
print(summary(df))